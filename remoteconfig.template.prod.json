{
  "parameters": {
    "health_sample_message": {
      "defaultValue": {
        "value": "all-good-prod"
      },
      "description": "Message returned by the health endpoint"
    },
    "health_env_label": {
      "defaultValue": {
        "value": "prod"
      },
      "description": "Label indicating the current environment (e.g., local, nonprod, prod)"
    },
    "require_auth": {
      "defaultValue": {
        "value": "false"
      },
      "description": "If true, the health function requires a valid Firebase Auth ID token and admin allowlist"
    },
    "admin_emails": {
      "defaultValue": {
        "value": "tazabreu@gmail.com"
      },
      "description": "Comma-separated list of admin emails allowed to invoke when require_auth is true"
    }
  }
}
