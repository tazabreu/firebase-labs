# Template for environment configuration - copy to .env.integration-test.[environment]
# This file is checked into git as a template, actual environment files are gitignored

# Target API configuration
INTEGRATION_TEST_TARGET_API=<API_BASE_URL>
INTEGRATION_TEST_TARGET_HEALTH_ENDPOINT=<ENDPOINT_PATH>

# Test configuration
TEST_TIMEOUT=5000
MONITOR_INTERVAL=60000 