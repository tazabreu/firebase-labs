{
  "name": "health-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for the health endpoint",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "monitor": "ts-node src/monitor.ts",
    "test:local": "cross-env ENV=local jest",
    "test:nonprod": "cross-env ENV=nonprod jest",
    "monitor:local": "cross-env ENV=local ts-node src/monitor.ts",
    "monitor:nonprod": "cross-env ENV=nonprod ts-node src/monitor.ts"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.0.0",
    "@types/node-fetch": "^2.6.12",
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
} 